import { StyleSheet } from 'react-native'
import { COLOR_WHITE } from './base-colors'

const baseStyles = StyleSheet.create({
  page: {
    width: '100%',
    backgroundColor: COLOR_WHITE
  }
})

export default baseStyles