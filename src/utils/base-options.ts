import { NativeStackNavigationOptions } from '@react-navigation/native-stack'
import { BottomTabNavigationOptions } from '@react-navigation/bottom-tabs'

export const baseScreenOptions: NativeStackNavigationOptions = {
  headerShown: false
}

export const baseTabNavigationOptions: BottomTabNavigationOptions = {
  headerShown: false
}