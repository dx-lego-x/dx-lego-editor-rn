const localStorage = {
  getItem: (key: string) => {
    return ''
  },

  setItem: (key: string, value: string) => {
    
  },

  removeItem: (key: string) => {
    
  }
}

export default localStorage